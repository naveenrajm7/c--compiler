/* A C minus program */

int gcd (int u,int v)
{
	if(v==0) return u;
	else return gcd(v,u-u/v*v); 
/* u-u/v*v == u mod v
hello */
return x;
}

void main(void)
{
	int x;int y; // /*/ My single line comment
	if(v!=u);
	x=input();y=input();
	output(gcd(x,y));
}
	
